pipeline {
    agent any

    stages {
        stage('Stage one') {
            steps {
                script {
                    docker.build "IMAGE1"
                }
            }
        }
    }
}
